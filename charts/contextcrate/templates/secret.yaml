{{- if .Values.secrets }}
apiVersion: v1
kind: Secret
metadata: { name: {{ include "contextcrate.fullname" . }} }
type: Opaque
stringData:
  {{- range $name, $value := .Values.secrets }}
  {{ $name }}: {{ $value | quote }}
  {{- end }}
{{- end }}
